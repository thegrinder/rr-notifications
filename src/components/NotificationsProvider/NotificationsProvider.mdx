---
name: NotificationsProvider
---

import { Props } from 'docz';
import NotificationsProvider from './NotificationsProvider';

# NotificationsProvider

NotificationsProvider component is just a context provider that takes a few configuration props.
The most important is renderNotification that allows for maximum flexibility when creating your
own custom notification component.

<Props of={NotificationsProvider} />